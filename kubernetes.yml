- name: Basic setup
  hosts:
    - servers
    - workers
  gather_facts: false
  vars:
    # Certificates
    certificates_kubernetes_cluster:
      domain: kubernetes.local
      endpoint: server
  roles:
    - server
    - certificates

- name: Install etcd
  hosts:
    - servers
  gather_facts: false
  roles:
    - etcd
