- name: Basic setup
  hosts:
    - servers
    - workers
  gather_facts: false
  vars:
    # Certificates
    certificates_kubernetes_cluster:
      domain: kubernetes.local
      endpoint: server
  roles:
    - server
    - certificates
