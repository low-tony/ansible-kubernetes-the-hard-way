- name: Basic setup
  hosts:
    - servers
    - nodes
  gather_facts: false
  vars:
    # Server
    server_custom_packages: []
    # Certificates
    certificates_kubernetes_cluster:
      domain: kubernetes.local
      endpoint: server
  roles:
    - server
    - certificates
